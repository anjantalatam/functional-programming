const users = ["anjan", "talatam"];

// forEach, map, reduce ..... are higher order functions
users.forEach((user) => console.log(user));

// another example of higher order function
setTimeout(() => console.log("Hi"), 5000); // Hi will be consoled after  5 seconds
